我想要从我的网页构建 PDF，我现在使用 ci 环境 node 调用 chrome 打印到 PDF 文件，然后用 lualatex 插入 PDF 构建。但是我觉得这样太不灵活且不好维护修改。我想要改为，打印到 PDF 之后，用 xelatex 构建首页、扉页、目录、尾页、章首页等等，然后用 python 操控 PDF 合并并植入目录。另外我还想把打印到 PDF 中的目录（node 端是自动从网页中的 h1~h6 提取并插入 PDF 了，包含了仅有的一个 H1 作为第一级目录）植入目标文件下面。我现在有几个问题，（1）python 操控 PDF 用什么库，告诉我各个库的优点缺点，以及你认为哪个更符合我的要求（2）xelatex 构建页面，你觉得是一个一个页面构建出来若干 PDF 在 python 中合并，还是构建出来所有页面合在一起的 PDF 在 Python 端中插入页面到相应的位置，还是构建出来包含若干空页（我在打印到 PDF 的时候可以用 node 直接输出每个 PDF 有几页，空页面直接用 Python 覆盖源 PDF，哪个更好，更方便我精确且灵活的插入目录、构建精确到垂直高度（例如 H2 不一定在页面最上方，我希望定位到 H2 原来的位置，也就是源 PDF 中的垂直高度）。