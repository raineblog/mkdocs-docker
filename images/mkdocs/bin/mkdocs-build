#!/bin/bash
set -e

cp /app/package.json .
cp /app/gulpfile.js .
cp -r /app/node_modules .

python /app/scripts/generate.py "$@"

mkdocs build --clean
cp -af public/. site/
npx gulp --input-dir site --output-dir dist
