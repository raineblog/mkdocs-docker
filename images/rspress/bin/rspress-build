#!/bin/bash
set -e

cp -r /app/styles .
cp -r /app/utils .
cp -r /app/node_modules .

cp /app/gulpfile.cjs .
cp /app/package.json .
cp /app/rspress.config.ts .
cp /app/tsconfig.json .

npm run build "$@"
